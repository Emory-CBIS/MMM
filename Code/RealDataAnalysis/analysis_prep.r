# ------------------------
# This script is used to load useful function in summarizing the results
# -------------------------
library(gplots)
library(circlize) # for colorbar
library(ComplexHeatmap) #for function Heatmap
path <- "./Code/RealDataAnalysis/"
source(paste(path, "analysis_help.r", sep = ""))